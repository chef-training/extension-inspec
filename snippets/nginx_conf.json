{
    ".source.inspec": {
      "Describe nginx_conf": {
        "prefix": "nginx_conf",
        "body": "# http://inspec.io/docs/reference/resources/nginx_conf\ndescribe nginx_conf('${1:name}') do\n  it { should${2:_not} ${3:exist} }${4}\nend"
      }
    }
  }
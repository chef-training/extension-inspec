{
    ".source.inspec": {
      "Describe postgres": {
        "prefix": "postgres",
        "body": "# http://inspec.io/docs/reference/resources/postgres\ndescribe postgres('${1:name}') do\n  it { should${2:_not} ${3:exist} }${4}\nend"
      }
    }
  }
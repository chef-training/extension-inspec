{
    ".source.inspec": {
      "Describe docker": {
        "prefix": "docker",
        "body": "# http://inspec.io/docs/reference/resources/docker\ndescribe docker('${1:name}') do\n  it { should${2:_not} ${3:exist} }${4}\nend"
      }
    }
  }
{
    ".source.inspec": {
      "Describe postgres_ident_conf": {
        "prefix": "postgres_ident_conf",
        "body": "# http://inspec.io/docs/reference/resources/postgres_ident_conf\ndescribe postgres_ident_conf('${1:name}') do\n  it { should${2:_not} ${3:exist} }${4}\nend"
      }
    }
  }
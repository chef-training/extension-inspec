{
  ".source.inspec": {
    "Describe security_policy": {
      "prefix": "security_policy",
      "body": "# http://inspec.io/docs/reference/resources/security_policy/\ndescribe security_policy do\n  its('${1:policy_name}') { should${2:_not} eq '${3:value}' }${4}\nend"
    }
  }
}
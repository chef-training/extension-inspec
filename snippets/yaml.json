{
  ".source.inspec": {
    "Describe yaml": {
      "prefix": "yaml",
      "body": "# http://inspec.io/docs/reference/resources/yaml/\ndescribe yaml('${1:file_name}') do\n  its('${2:setting}') { should${3:_not} eq '${4:value}' }${5}\nend"
    }
  }
}
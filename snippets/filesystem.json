{
    ".source.inspec": {
      "Describe filesystem": {
        "prefix": "filesystem",
        "body": "# http://inspec.io/docs/reference/resources/filesystem\ndescribe filesystem('${1:name}') do\n  it { should${2:_not} ${3:exist} }${4}\nend"
      }
    }
  }